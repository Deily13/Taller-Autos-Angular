import { CheckboxMarcasDirective } from './checkbox-marcas.directive';

describe('CheckboxMarcasDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxMarcasDirective();
    expect(directive).toBeTruthy();
  });
});
