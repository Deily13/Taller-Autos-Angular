import { AnimationSaludoDirective } from './animation-saludo.directive';

describe('AnimationSaludoDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationSaludoDirective();
    expect(directive).toBeTruthy();
  });
});
